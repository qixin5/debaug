#!/bin/bash

rootdir=${DEBAUG_DIR} 

call_graph_file=$1 #callgraph.dot generated by clang
line_file=$2 #line.txt


java -cp :${rootdir}/build/java:${rootdir}/lib/java/* edu.gatech.cc.debaug.FunctionCallRelationGenerator ${call_graph_file} ${line_file}

